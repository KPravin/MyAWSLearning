# Network ACLs (NACL)

This folder contains AWS Network ACLs examples and implementations.

## Overview

A network access control list (NACL) is an optional layer of security for your VPC that acts as a firewall for controlling traffic in and out of subnets.

## Key Features

- Stateless firewall rules
- Subnet-level access control
- Inbound and outbound rules

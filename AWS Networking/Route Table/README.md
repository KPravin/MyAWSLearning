# Route Table

This folder contains AWS Route Table examples and implementations.

## Overview

A route table is a set of rules (called routes) that determines where network traffic from your subnet or gateway is directed.

## Key Features

- Route definitions
- Subnet associations
- Route targets (NAT Gateway, Internet Gateway, etc.)
